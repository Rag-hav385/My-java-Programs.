import java.io.File;
import java.io.PrintWriter;

public class learnings4 
{
    // readind and writting a file
    /*
    
           File file = new File("filename.txt"); // make  file 

           if(!file.exists())
            {
                file.createNewFile();
            }
            PrintWriter pw = new PrintWriter(file); // to write in a file
             
            pw.println(".......");

            // for reading we need java.io.BufferdReader
            //  java.io.FileReader;
            //  java.io.IOException;
            //code//

            BufferReader br = 0
            try {
                br = new BufferedReader(new FileReader("fileName.txt"));

                string line;

                while ((line = br.readLine())!= null)
                {
                    syso(line);
                }
            }catch (IOException e) {
                e.printStackTrace();
            }
        }
             finally{
                 br.close();
             }
 */
                 // date time //

    /*
     java.util.Date
     Date date = new Date(); // initialisation
     System.out.println(date.toString());
     // has many functions or methods//

     

          */
}
